# Kami · 项目纪要

- 项目目标：启动并维护 awesome-gpt-image-2 项目。
- 约束：遵循文档驱动、实时决策记录与高频 Git 协作同步。

## 当前状态
- ✅ 已完成：2026-08-17 已定位公网页面异常根因：静态入口正常，但前端根路径资源/API/路由未适配 `/gpt-image-2/` 子路径。
- ▶️ 进行中：服务器 Nginx 仍通过 80 端口提供 `/gpt-image-2/`，当前 release 为 `/opt/awesome-gpt-image-2/releases/20260817-1038`；本轮仅诊断，未改生产配置。
- ⏸️ 待办：处理依赖审计发现的 3 个高危项（需单独评估升级影响）。
- ❓ 待确认：用户是否授权修复子路径兼容并重新部署。

## 决策记录（实时追加，宁滥勿缺）
- [08-17-2026 11:09:02] 选择统一基础路径方案｜背景：子路径异常可通过前端统一 base path、Nginx 根路径兼容或独立端口三种方式处理；后两者分别会污染现有电商站点或受阿里云安全组限制｜结论：用户选择方案 A，新增统一基础路径工具，覆盖静态数据、图片、API、社区导航与路由识别，并保持根路径部署兼容｜来源：用户
- [08-17-2026 11:08:10] 授权修复并重新部署｜背景：已确认公网异常由应用未适配 `/gpt-image-2/` 子路径导致，修复需要修改前端路径逻辑并更新生产服务器 release｜结论：用户明确授权本轮修复子路径兼容并重新部署到 `47.116.40.121`；部署后需同时验证新站点、API 隔离及原电商首页不受影响｜来源：用户
- [08-17-2026 10:52:26] 部署访问异常根因｜背景：公网入口与静态 JS/CSS 均返回 200，但浏览器请求的 `/cases.json`、`/style-library.json` 被服务器根站点接管并返回 853 字节 HTML，源码同时使用根路径 `/api/*` 与 `/community`｜结论：异常由应用未适配 `/gpt-image-2/` 子路径导致；修复应统一使用 Vite base 派生的数据、API、图片和社区路由，或改为独立域名/端口根部署，本轮未改生产配置｜来源：AI
- [08-17-2026 10:50:14] 调查部署页面访问异常｜背景：用户反馈 http://47.116.40.121/gpt-image-2/ 访问有问题，要求查看｜结论：本轮先从公网浏览器、HTTP 资源与 Nginx 日志复现并定位根因；未经进一步授权不修改生产配置｜来源：用户
- [08-17-2026 10:27:26] 部署到指定服务器｜背景：用户要求将当前项目部署到服务器 `47.116.40.121`，允许 AI 自主选择服务端口，并要求通过 IP 直接访问。｜结论：本轮获得对该服务器执行部署的明确许可；先核验项目部署形态、SSH 连接与服务器现状，再选择不冲突端口完成部署和外网 HTTP 验证。｜来源：用户
- [08-17-2026 10:31:59] 选择静态 Nginx 部署与 8088 端口｜背景：服务器已有 Nginx 占用 80、443、8010，项目生产 API 原设计依赖 Vercel 与未提供的生产密钥；覆盖既有端口或伪装 API 可用都会引入风险。｜结论：使用独立端口 `8088` 和 `/opt/awesome-gpt-image-2` 版本化目录部署 Vite 静态图库；Nginx 对 `/api/` 明确返回 503 `API_NOT_CONFIGURED`，不修改既有服务，也不上传生产密钥。｜来源：AI
- [08-17-2026 10:37:24] 因安全组限制改用 80 端口子路径｜背景：8088 在服务器本机返回 200，但公网 TCP 连接失败；主机防火墙未启用，且实例没有可用阿里云 CLI 凭据或 RAM 角色，无法在服务器内放行云安全组。公网 80 端口可达且已有 ecommerce-bi 站点，不能覆盖其首页。｜结论：将最终访问地址调整为 `http://47.116.40.121/gpt-image-2/`，使用已开放的 80 端口，在现有 Nginx 默认站点中新增隔离路径并保留原首页/API；重新以 `/gpt-image-2/` 为 Vite base 构建，停用仅内网可达的 8088 配置。｜来源：AI
- [08-17-2026 10:41:01] 完成 IP 子路径部署｜背景：以 `/gpt-image-2/` 为 base 的生产构建已上传到版本化 release，并将 Nginx 默认站点增加隔离路径；需要确认新站点和原 ecommerce-bi 站点均可用。｜结论：最终地址为 `http://47.116.40.121/gpt-image-2/`，当前 release 为 `20260817-1038`；外网首页、JS 资源和 `/gpt-image-2/community` 均返回 200，原 IP 首页仍返回“电商BI数据看板”；未配置的 `/gpt-image-2/api/*` 明确返回 503 `API_NOT_CONFIGURED`。｜来源：AI
- [08-16-2026 17:59:58] 启动本地项目｜背景：用户再次要求“启动项目”，需要核验现有开发服务并在服务未运行时重新启动。｜结论：检查 5174 端口和项目进程，必要时使用现有依赖启动 Vite，并以 HTTP 200 和页面标题完成验证。｜来源：用户
- [08-16-2026 13:52:25] 调查未经明确请求的插件安装｜背景：用户发现 awesome-gpt-image-2 已显示为 Codex 插件，但不记得发起过将项目制作为或安装为插件的指令，要求查清发生时间与来源。｜结论：本轮仅进行只读取证，核对插件缓存、清单、配置和文件时间，不卸载、不修改插件状态；结论必须区分已验证事实与推断。｜来源：用户
- [08-16-2026 13:56:30] 插件安装来源判定｜背景：插件缓存创建于 2026-08-16 10:08:53，Codex 清单生成于 10:09:05；同日 10:10:51 的任务消息显式附带 `plugin://gpt-image-2-style-library@awesome-gpt-image-2`，但所有 8 月 16 日任务日志中不存在实际插件安装工具调用。缓存 Git HEAD 与本地工作区 HEAD `0f5618c` 完全一致，而远端仍停在 `76fcd0e`，证明缓存取自本地项目而非远端安装。｜结论：可确认不是 8 月 7 日或 11 日“启动项目”命令导致，也不是 Agent 调用安装工具；最符合证据的机制是用户在 10:10 任务发送前通过 Codex 界面选择/附加该本地插件时，桌面端自动完成本地缓存和清单转换。该界面触发机制属于证据推断，日志未记录具体点击动作。｜来源：AI
- [08-11-2026 09:29:27] 再次启动本地项目｜背景：用户再次要求“启动项目”，需要检查历史开发服务是否仍存活，并在必要时重新拉起。｜结论：核验当前监听进程与页面响应；若服务已停止，则使用现有依赖重新启动当前项目并验证访问。｜来源：用户
- [08-11-2026 09:29:57] 复用本地访问端口｜背景：历史服务已停止，5173 与 5174 当前均空闲；此前已向用户交付 5174 作为本项目访问地址。｜结论：继续使用 `127.0.0.1:5174` 启动，保持本地访问地址稳定。｜来源：AI
- [08-07-2026 13:38:55] 启动本地项目｜背景：用户要求“启动项目”，需要运行当前工作区应用并确认可用。｜结论：在当前工作区识别官方启动命令，启动本地开发服务并验证访问。｜来源：用户
- [08-07-2026 13:39:56] 本地启动端口与命令｜背景：默认端口 5173 已由 sales_engine 项目的 Vite 服务占用，且 PowerShell 执行策略禁止 npm.ps1；直接复用或终止会影响无关项目。｜结论：保留现有进程，使用 npm.cmd 安装依赖，并在 127.0.0.1:5174 启动当前项目。｜来源：AI
- [08-07-2026 13:41:50] 依赖审计处置边界｜背景：`npm ci` 成功安装 249 个包，但 npm audit 报告 3 个高危项；自动执行 `npm audit fix` 可能改变依赖版本和运行行为，超出本次仅启动项目的目标。｜结论：本轮不自动升级依赖，先保证项目启动并将安全项列为后续待办。｜来源：AI
- [08-07-2026 13:42:39] 忽略生成文件伪变更｜背景：启动前置脚本重写 `data/cases.json` 与样式库引用文件后，Git 因行尾元数据标记为修改，但 `git diff --exit-code` 返回 0，证明规范化内容未变化。｜结论：仅通过 `git add` 刷新索引，使两文件恢复干净状态，不提交无实际内容的变更。｜来源：AI
- [08-07-2026 13:43:33] Git 远端同步受阻｜背景：项目纪要已本地提交为 `54e57a5`，但执行 `git push` 时远端明确返回当前凭据 `greendx42` 对 `freestylefly/awesome-gpt-image-2` 无写权限（HTTP 403）。｜结论：保留本地提交，不重复相同推送；待切换为有权限的 GitHub 凭据或由仓库维护者授权后再同步。｜来源：AI

## 待解决问题
- 修复 `/gpt-image-2/` 子路径下的数据、图片、API 与社区路由，然后经用户当次许可重新部署。
- 评估 `npm audit` 报告的 3 个高危依赖项及兼容升级方案。
- 使用具备仓库写权限的 GitHub 凭据推送本地纪要提交。
- 用户决定是否卸载 `gpt-image-2-style-library@awesome-gpt-image-2` 插件。

## 工作文件集
- `doc/项目纪要-Kami.md`
- `docs/superpowers/plans/2026-08-17-deploy-ip-server.md`
- `package.json`
- `package-lock.json`
- `README.md`
- 文档同步：本轮未修改业务代码、页面、配置或产品需求，无业务文档影响；已同步本项目纪要。
